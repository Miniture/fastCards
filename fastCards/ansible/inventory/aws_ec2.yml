plugin: amazon.aws.aws_ec2
regions:
  - us-east-2

filters:
  "tag:Project": "fastcards"
  instance-state-name: running

hostnames:
  - ip-address

keyed_groups:
  - key: tags.Role
    prefix: role